Before running requirements file download
       PyAudio‑0.2.11‑cp37‑cp37m‑win_amd64.whl from the url below
       https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
once you Download the file
- press windows + R on keyboard type "cmd"
- cd Downloads
- pip install PyAudio‑0.2.11‑cp37‑cp37m‑win_amd64.whl
- pip install -r requirements.txt

This will install all the dependencies the you can run the script without any errors.


Running model

# we are running two models parallely to accomplish the text from the model
- Object Detection model
- Neural Image Captioning using the VGG16 & LSTM

Simillarly we are using Google Vision API to link with the code .

-Set the google cloud vision api and create credentials and Download the credentials API key into the local to access the api from the local.

